#!/bin/bash
if [ -f ~/.cgenierc ]; then
    echo 'cTOASTER is already set up -- if you want to start over, remove ~/.cgenierc ($ rm ~/.cgenierc)'
    exit 0
fi
#source tools/find_python
#alias python='/usr/bin/python3'
exec python3 tools/setup-cgenie.py
