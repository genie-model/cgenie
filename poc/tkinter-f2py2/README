On Ubuntu 18.04 I needed to install the packages 'python-devel' and 'python-pandas':
- sudo apt-get install python-dev
- sudo apt-get install python-pandas

To run, type:
make
python gui.py

Some notes:
A straightforward guide to tkinter basics is at:
https://realpython.com/python-gui-tkinter/

